# Commitment of Traders 그룹 (v0.23.0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** 신규 `cot/` 패키지 3 endpoint, 3 구조체(COTReport 124필드는 카탈로그에서 도출).

**Architecture:** internal/fetch(List) + filterParams. COTAnalysis/COTList 는 스펙 verbatim, COTReport 는 카탈로그 JSON 전수 매핑.

참고: `unset GOROOT`. 커밋 한국어 `feat(cot): ...`. 테스트 헬퍼는 calendar 패턴 패키지 내부 정의.

---

### Task 1: 패키지 + COTAnalysis + COTList (작은 2종)

**Files:** Create `cot/client.go`, `cot/cot.go`(COTAnalysis/COTList struct + Analysis/List method + filterParams), `cot/cot_test.go`, testdata `analysis.json`, `list.json`.

- [ ] **Step 1:** `cot/client.go`:
```go
// Package cot 는 FMP 상품선물 COT(Commitment of Traders) 리포트 API sub-client.
// fmp.Client.COT 로 접근.
package cot

import "github.com/kenshin579/fmp-go/internal/httpclient"

type Client struct {
	http *httpclient.Client
}

func New(http *httpclient.Client) *Client { return &Client{http: http} }

func filterParams(symbol, from, to string) map[string]string {
	q := map[string]string{}
	if symbol != "" {
		q["symbol"] = symbol
	}
	if from != "" {
		q["from"] = from
	}
	if to != "" {
		q["to"] = to
	}
	return q
}
```
- [ ] **Step 2:** `cot/cot.go` — 스펙 `COTAnalysis`(16필드)/`COTList`(2필드) struct + `Analysis`/`List` 메서드. COTReport struct + Report 메서드는 Task 2 에서 추가(같은 파일). 지금은 Analysis/List 만:
```go
func (c *Client) Analysis(ctx context.Context, symbol, from, to string) ([]COTAnalysis, error) {
	return fetch.List[COTAnalysis](ctx, c.http, "/stable/commitment-of-traders-analysis", filterParams(symbol, from, to))
}
func (c *Client) List(ctx context.Context) ([]COTList, error) {
	return fetch.List[COTList](ctx, c.http, "/stable/commitment-of-traders-list", nil)
}
```
import: context, internal/fetch.
- [ ] **Step 3:** fixtures: analysis.json `[{symbol:"ES",date,name,sector,exchange,currentLongMarketSituation:55.2,currentShortMarketSituation:44.8,marketSituation:"Bullish",previousLongMarketSituation:50.1,previousShortMarketSituation:49.9,previousMarketSituation:"Neutral",netPostion:12345,previousNetPosition:10000,changeInNetPosition:23.45,marketSentiment:"Bullish",reversalTrend:false}]`, list.json `[{symbol:"ES",name:"E-MINI S&P 500"}]`.
- [ ] **Step 4:** `cot_test.go`(헬퍼 calendar 패턴 정의): Analysis 파싱(NetPostion!=0 — 오타 키, ReversalTrend==false, MarketSentiment!="") + delegation(path+symbol/from/to) / List 파싱(Symbol/Name) + path.
- [ ] **Step 5:** `unset GOROOT && go test ./cot/ && go vet ./cot/ && gofmt -l cot/`. 커밋 `feat(cot): COTAnalysis + COTList`.

### Task 2: COTReport (124필드, 카탈로그 도출)

**Files:** Modify `cot/cot.go`(COTReport struct + Report method 추가), `cot/cot_test.go`(Report 테스트 추가); Create testdata `report.json`.

- [ ] **Step 1:** `docs/api/commitmentOfTraders/cot-report.md` 의 JSON 응답 예시를 READ. 예시 객체의 **모든 키**를 `COTReport` struct 필드로 매핑(faithful). 타입 규칙:
  - 텍스트(symbol/date/name/sector/marketAndExchangeNames/cftc*Code/contractUnits) → string
  - openInterest*/*Positions*/change*/traders* → int64
  - pctOf*/conc* → float64
  - FMP 키 그대로 보존: `changeInNoncommSpeadAll`(오타), `tradersNoncommSpeadOl`(오타), pct·trader old 블록 접미사 `Ol`, 포지션 블록 접미사 `Old`.
  Go 필드명은 관례대로(예: `OpenInterestAll`, `PctOfOiNoncommLongAll`, `ChangeInNoncommSpeadAll`, `TradersNoncommSpeadOl`). struct 주석 `// COTReport — COT 전체 포지션 리포트 (commitment-of-traders-report)`.
- [ ] **Step 2:** Report 메서드:
```go
func (c *Client) Report(ctx context.Context, symbol, from, to string) ([]COTReport, error) {
	return fetch.List[COTReport](ctx, c.http, "/stable/commitment-of-traders-report", filterParams(symbol, from, to))
}
```
- [ ] **Step 3:** fixture `report.json` — 카탈로그 cot-report.md 의 JSON 예시 **그대로**(1건 배열, 전체 키 포함).
- [ ] **Step 4:** Report 테스트: 파싱 후 대표 필드 검증 — OpenInterestAll!=0(int), PctOfOpenInterestAll!=0(float), ChangeInNoncommSpeadAll(오타 키 파싱 — 값 존재/타입), Name!="" , Symbol!="". delegation(path `/stable/commitment-of-traders-report`+symbol/from/to). 카탈로그 JSON 의 모든 키가 struct 에 매핑됐는지(누락 키로 인한 무시 없는지) — 최소 각 블록 대표 1개씩 비0 검증.
- [ ] **Step 5:** `unset GOROOT && go test ./cot/ && go vet ./cot/ && gofmt -l cot/`. 커밋 `feat(cot): COTReport (124필드)`.

### Task 3: 루트 와이어 + README + examples + 통합테스트

**Files:** Modify `client.go`, `client_test.go`, `README.md`, `integration_test.go`; Create `examples/cot/main.go`.

- [ ] **Step 1:** `client.go` — import `cot`, struct 에 `COT *cot.Client`, NewClient 에 `c.COT = cot.New(hc)`. (gofmt -w client.go.)
- [ ] **Step 2:** `client_test.go` — `TestNewClient_HasCOT`.
- [ ] **Step 3:** README 표 행 신규: `| Commitment of Traders | \`client.COT\` | Report, Analysis, List — 3 endpoint |`.
- [ ] **Step 4:** `examples/cot/main.go` — NewClientFromEnv → List(0) 건수 + Analysis("ES","","") 첫 건 시장심리 출력.
- [ ] **Step 5:** `integration_test.go` — `TestIntegration_COT`: List len>0 / Analysis("ES","","") err 체크 / Report("ES","","") err 체크.
- [ ] **Step 6:** `unset GOROOT && go build ./... && go vet ./... && go test ./... && go build -tags integration ./... && gofmt -l .` 통과. 커밋 `feat(cot): 루트 와이어 + README + examples + 통합테스트`.

## Self-Review 메모
- 3 endpoint: Analysis/List=T1, COTReport(124)=T2, 와이어/문서=T3.
- COTReport 는 카탈로그 JSON 전수 매핑(오타/Ol 접미사 보존). COTAnalysis netPostion 오타 + reversalTrend bool.
- symbol/from/to 선택 필터(가드 없음).
