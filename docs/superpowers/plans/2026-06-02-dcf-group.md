# Discounted Cash Flow 그룹 (v0.19.0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** 신규 `dcf/` 패키지 4 endpoint, 3 구조체.

**Architecture:** internal/fetch(List). DCFValue(단순 2개 공유) + CustomDCFParams(18 *float64 override) + CustomDCFAdvanced/CustomDCFLevered. 구조체는 스펙 verbatim.

참고: `unset GOROOT`. 커밋 한국어 `feat(dcf): ...`. 테스트 헬퍼는 calendar 패턴 패키지 내부 정의. JSON 키 "Stock Price"/costofDebt 주의.

---

### Task 1: 패키지 scaffold + 단순 DCF 2종

**Files:** Create `dcf/client.go`, `dcf/dcf.go`, `dcf/dcf_test.go`, testdata `discounted-cash-flow.json`, `levered-discounted-cash-flow.json`.

- [ ] **Step 1:** `dcf/client.go`:
```go
// Package dcf 는 FMP DCF 밸류에이션 API sub-client.
// fmp.Client.DCF 로 접근.
package dcf

import "github.com/kenshin579/fmp-go/internal/httpclient"

type Client struct {
	http *httpclient.Client
}

func New(http *httpclient.Client) *Client { return &Client{http: http} }
```
- [ ] **Step 2:** `dcf/dcf.go` — 스펙 `DCFValue` struct + 2 메서드. import: context, fmt, strings, internal/fetch.
```go
func (c *Client) DiscountedCashFlow(ctx context.Context, symbol string) ([]DCFValue, error) {
	if strings.TrimSpace(symbol) == "" {
		return nil, fmt.Errorf("fmp: symbol must not be empty")
	}
	return fetch.List[DCFValue](ctx, c.http, "/stable/discounted-cash-flow", map[string]string{"symbol": symbol})
}
func (c *Client) LeveredDiscountedCashFlow(ctx context.Context, symbol string) ([]DCFValue, error) {
	if strings.TrimSpace(symbol) == "" {
		return nil, fmt.Errorf("fmp: symbol must not be empty")
	}
	return fetch.List[DCFValue](ctx, c.http, "/stable/levered-discounted-cash-flow", map[string]string{"symbol": symbol})
}
```
- [ ] **Step 3:** fixtures: discounted-cash-flow.json `[{"symbol":"AAPL","date":"2026-04-08","dcf":179.66,"Stock Price":225.5}]`, levered-discounted-cash-flow.json(같은 shape, 값 다름).
- [ ] **Step 4:** `dcf_test.go`(헬퍼 calendar 패턴 정의): DiscountedCashFlow("AAPL") 파싱(DCF!=0, StockPrice!=0 — "Stock Price" 키 매핑 확인) + delegation(path+symbol) / LeveredDiscountedCashFlow path / 빈 symbol 가드.
- [ ] **Step 5:** `unset GOROOT && go test ./dcf/ && go vet ./dcf/ && gofmt -l dcf/`. 커밋 `feat(dcf): 단순 DCF + 레버드 DCF`.

### Task 2: Custom DCF (advanced + levered)

**Files:** Create `dcf/custom.go`, `dcf/custom_test.go`, testdata `custom-discounted-cash-flow.json`, `custom-levered-discounted-cash-flow.json`.

- [ ] **Step 1:** `dcf/custom.go` — 스펙 `CustomDCFParams`(18 *float64) + queryParams + `CustomDCFAdvanced`/`CustomDCFLevered` struct + 2 메서드. import: context, fmt, strconv, strings, internal/fetch.
```go
func (p CustomDCFParams) queryParams() map[string]string {
	q := map[string]string{"symbol": p.Symbol}
	add := func(key string, v *float64) {
		if v != nil {
			q[key] = strconv.FormatFloat(*v, 'f', -1, 64)
		}
	}
	add("revenueGrowthPct", p.RevenueGrowthPct)
	add("ebitdaPct", p.EbitdaPct)
	add("depreciationAndAmortizationPct", p.DepreciationAndAmortizationPct)
	add("cashAndShortTermInvestmentsPct", p.CashAndShortTermInvestmentsPct)
	add("receivablesPct", p.ReceivablesPct)
	add("inventoriesPct", p.InventoriesPct)
	add("payablePct", p.PayablePct)
	add("ebitPct", p.EbitPct)
	add("capitalExpenditurePct", p.CapitalExpenditurePct)
	add("operatingCashFlowPct", p.OperatingCashFlowPct)
	add("sellingGeneralAndAdministrativeExpensesPct", p.SellingGeneralAndAdministrativeExpensesPct)
	add("taxRate", p.TaxRate)
	add("longTermGrowthRate", p.LongTermGrowthRate)
	add("costOfDebt", p.CostOfDebt)
	add("costOfEquity", p.CostOfEquity)
	add("marketRiskPremium", p.MarketRiskPremium)
	add("beta", p.Beta)
	add("riskFreeRate", p.RiskFreeRate)
	return q
}

func (c *Client) CustomDiscountedCashFlow(ctx context.Context, p CustomDCFParams) ([]CustomDCFAdvanced, error) {
	if strings.TrimSpace(p.Symbol) == "" {
		return nil, fmt.Errorf("fmp: symbol must not be empty")
	}
	return fetch.List[CustomDCFAdvanced](ctx, c.http, "/stable/custom-discounted-cash-flow", p.queryParams())
}
func (c *Client) CustomLeveredDiscountedCashFlow(ctx context.Context, p CustomDCFParams) ([]CustomDCFLevered, error) {
	if strings.TrimSpace(p.Symbol) == "" {
		return nil, fmt.Errorf("fmp: symbol must not be empty")
	}
	return fetch.List[CustomDCFLevered](ctx, c.http, "/stable/custom-levered-discounted-cash-flow", p.queryParams())
}
```
CustomDCFParams/CustomDCFAdvanced/CustomDCFLevered struct 본문은 스펙 "응답 타입" 절 verbatim.
- [ ] **Step 2:** fixtures: custom-discounted-cash-flow.json `[{...CustomDCFAdvanced 주요 필드, year:"2030", wacc:0.085, ufcf:..., equityValuePerShare:..., costofDebt:0.03}]`, custom-levered-discounted-cash-flow.json `[{...CustomDCFLevered, operatingCashFlow:..., freeCashFlow:..., wacc:...}]`.
- [ ] **Step 3:** `custom_test.go`(헬퍼 재사용): CustomDiscountedCashFlow(CustomDCFParams{Symbol:"AAPL"}) 파싱(Year!="", WACC!=0, EquityValuePerShare!=0, CostOfDebt 키 costofDebt 매핑) / CustomLeveredDiscountedCashFlow 파싱(FreeCashFlow!=0, OperatingCashFlow!=0) / delegation: helper `ptr := func(f float64) *float64 { return &f }`, CustomDiscountedCashFlow(CustomDCFParams{Symbol:"AAPL", Beta: ptr(1.2), TaxRate: ptr(0.21)}) → path `/stable/custom-discounted-cash-flow` + query symbol/beta=="1.2"/taxRate=="0.21" + revenueGrowthPct 키 부재 / 빈 Symbol 가드.
- [ ] **Step 4:** `go test ./dcf/ && go vet && gofmt -l`. 커밋 `feat(dcf): Custom DCF(advanced/levered) + 18 override`.

### Task 3: 루트 와이어 + README + examples + 통합테스트

**Files:** Modify `client.go`, `client_test.go`, `README.md`, `integration_test.go`; Create `examples/dcf/main.go`.

- [ ] **Step 1:** `client.go` — import `dcf`, struct 에 `DCF *dcf.Client`, NewClient 에 `c.DCF = dcf.New(hc)`. (gofmt -w client.go.)
- [ ] **Step 2:** `client_test.go` — `TestNewClient_HasDCF`.
- [ ] **Step 3:** README 표 행 신규: `| DCF | \`client.DCF\` | DiscountedCashFlow, LeveredDiscountedCashFlow, CustomDiscountedCashFlow, CustomLeveredDiscountedCashFlow — 4 endpoint |`.
- [ ] **Step 4:** `examples/dcf/main.go` — NewClientFromEnv → DiscountedCashFlow("AAPL") 내재가치 vs 현재가 출력 + CustomDiscountedCashFlow(dcf.CustomDCFParams{Symbol:"AAPL"}) 첫 해 WACC 출력. import dcf.
- [ ] **Step 5:** `integration_test.go` — `TestIntegration_DCF`: DiscountedCashFlow("AAPL") len>0 & DCF>0 / CustomDiscountedCashFlow(dcf.CustomDCFParams{Symbol:"AAPL"}) len>0 / CustomLeveredDiscountedCashFlow(dcf.CustomDCFParams{Symbol:"AAPL"}) len>0. import dcf.
- [ ] **Step 6:** `unset GOROOT && go build ./... && go vet ./... && go test ./... && go build -tags integration ./... && gofmt -l .` 통과. 커밋 `feat(dcf): 루트 와이어 + README + examples + 통합테스트`.

## Self-Review 메모
- 4 endpoint: 단순 2=T1, custom 2=T2, 와이어/문서=T3.
- DCFValue "Stock Price" 키, custom costofDebt 소문자 키 주의.
- CustomDCFParams 18 override 미설정 시 쿼리 제외(테스트로 확인).
- CustomDCFAdvanced vs CustomDCFLevered 필드셋 다름(별도 struct).
