# ESG 그룹 (v0.21.0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** 신규 `esg/` 패키지 3 endpoint, 3 구조체.

**Architecture:** internal/fetch(List). 구조체는 스펙 verbatim. path 주의: esg-disclosures.

참고: `unset GOROOT`. 커밋 한국어 `feat(esg): ...`. 테스트 헬퍼는 calendar 패턴 패키지 내부 정의.

---

### Task 1: 패키지 + 3 endpoint

**Files:** Create `esg/client.go`, `esg/esg.go`, `esg/esg_test.go`, testdata `esg-ratings.json`, `esg-disclosures.json`, `esg-benchmark.json`.

- [ ] **Step 1:** `esg/client.go`:
```go
// Package esg 는 FMP ESG 평가/공시/벤치마크 API sub-client.
// fmp.Client.ESG 로 접근.
package esg

import "github.com/kenshin579/fmp-go/internal/httpclient"

type Client struct {
	http *httpclient.Client
}

func New(http *httpclient.Client) *Client { return &Client{http: http} }
```
- [ ] **Step 2:** `esg/esg.go` — 스펙 `ESGRating`/`ESGDisclosure`/`ESGBenchmark` struct + 3 메서드. import: context, fmt, strings, internal/fetch.
```go
func (c *Client) Ratings(ctx context.Context, symbol string) ([]ESGRating, error) {
	if strings.TrimSpace(symbol) == "" {
		return nil, fmt.Errorf("fmp: symbol must not be empty")
	}
	return fetch.List[ESGRating](ctx, c.http, "/stable/esg-ratings", map[string]string{"symbol": symbol})
}
func (c *Client) Disclosures(ctx context.Context, symbol string) ([]ESGDisclosure, error) {
	if strings.TrimSpace(symbol) == "" {
		return nil, fmt.Errorf("fmp: symbol must not be empty")
	}
	return fetch.List[ESGDisclosure](ctx, c.http, "/stable/esg-disclosures", map[string]string{"symbol": symbol})
}
func (c *Client) Benchmark(ctx context.Context, year string) ([]ESGBenchmark, error) {
	if strings.TrimSpace(year) == "" {
		return nil, fmt.Errorf("fmp: year must not be empty")
	}
	return fetch.List[ESGBenchmark](ctx, c.http, "/stable/esg-benchmark", map[string]string{"year": year})
}
```
- [ ] **Step 3:** fixtures: esg-ratings.json `[{symbol:"AAPL",cik:"0000320193",companyName,industry,fiscalYear:2024,ESGRiskRating:"B",industryRank:"4 out of 5"}]`, esg-disclosures.json `[{date,acceptedDate,symbol:"AAPL",cik,companyName,formType:"8-K",environmentalScore:52.52,socialScore:45.18,governanceScore:60.74,ESGScore:52.81,url}]`, esg-benchmark.json `[{fiscalYear:2023,sector:"APPAREL RETAIL",environmentalScore:61.36,socialScore:67.44,governanceScore:68.1,ESGScore:65.63}]`.
- [ ] **Step 4:** `esg_test.go`(헬퍼 calendar 패턴 정의): Ratings 파싱(FiscalYear==2024, ESGRiskRating!="", IndustryRank!="") + delegation(path+symbol) / Disclosures 파싱(ESGScore!=0, EnvironmentalScore!=0) + delegation(path `/stable/esg-disclosures`+symbol) / Benchmark 파싱(Sector!="", ESGScore!=0) + delegation(path+year) / 가드 Ratings 빈 symbol + Benchmark 빈 year.
- [ ] **Step 5:** `unset GOROOT && go test ./esg/ && go vet ./esg/ && gofmt -l esg/`. 커밋 `feat(esg): ESG 평가/공시/벤치마크 3종`.

### Task 2: 루트 와이어 + README + examples + 통합테스트

**Files:** Modify `client.go`, `client_test.go`, `README.md`, `integration_test.go`; Create `examples/esg/main.go`.

- [ ] **Step 1:** `client.go` — import `esg`, struct 에 `ESG *esg.Client`, NewClient 에 `c.ESG = esg.New(hc)`. (gofmt -w client.go.)
- [ ] **Step 2:** `client_test.go` — `TestNewClient_HasESG`.
- [ ] **Step 3:** README 표 행 신규: `| ESG | \`client.ESG\` | Ratings, Disclosures, Benchmark — 3 endpoint |`.
- [ ] **Step 4:** `examples/esg/main.go` — NewClientFromEnv → Ratings("AAPL") 등급 + Benchmark("2023") 건수 출력.
- [ ] **Step 5:** `integration_test.go` — `TestIntegration_ESG`: Ratings("AAPL") len>0 / Disclosures("AAPL") err 체크 / Benchmark("2023") len>0.
- [ ] **Step 6:** `unset GOROOT && go build ./... && go vet ./... && go test ./... && go build -tags integration ./... && gofmt -l .` 통과. 커밋 `feat(esg): 루트 와이어 + README + examples + 통합테스트`.

## Self-Review 메모
- 3 endpoint, 3 struct. ESGScore 대문자 키. fiscalYear int. esg-disclosures path.
- Ratings/Disclosures symbol 가드, Benchmark year 가드.
