# Form 13F 그룹 (v0.25.0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** 신규 `form13f/` 패키지 8 endpoint, 8 구조체(대형 다수, 카탈로그 도출).

**Architecture:** internal/fetch(List) + pageParams. 각 struct 는 해당 카탈로그 JSON 전수 매핑. 타입 규칙: 금액/주식/카운트/value/절대performance → int64, *Percentage/weight*/ratio/price/ownership*/turnover* → float64, bool 3종 bool, 텍스트 string, dates#3 year/quarter int64.

참고: `unset GOROOT`. 커밋 한국어 `feat(form13f): ...`. 테스트 헬퍼는 calendar 패턴 패키지 내부 정의.

---

### Task 1: 패키지 + 작은 4종 (Filing/Holding/FilingDate/IndustrySummary)

**Files:** Create `form13f/client.go`, `form13f/filings.go`, `form13f/filings_test.go`, testdata `latest.json`, `extract.json`, `dates.json`, `industry-summary.json`.

- [ ] **Step 1:** `form13f/client.go`:
```go
// Package form13f 는 FMP 기관 13F 보유/분석 API sub-client.
// fmp.Client.Form13F 로 접근.
package form13f

import (
	"strconv"

	"github.com/kenshin579/fmp-go/internal/httpclient"
)

type Client struct {
	http *httpclient.Client
}

func New(http *httpclient.Client) *Client { return &Client{http: http} }

func pageParams(page, limit int) map[string]string {
	q := map[string]string{"page": strconv.Itoa(page)}
	if limit > 0 {
		q["limit"] = strconv.Itoa(limit)
	}
	return q
}
```
- [ ] **Step 2:** 각 카탈로그(latest-filings.md, filings-extract.md, form-13f-filings-dates.md, industry-summary.md) JSON 예시 READ → `form13f/filings.go` 에 `Filing`/`Holding`/`FilingDate`/`IndustrySummary` struct(전수 매핑) + 4 메서드. 타입: Holding 의 shares/value int64, FilingDate 의 year/quarter int64, IndustrySummary 의 industryValue int64, 나머지 string.
```go
func (c *Client) LatestFilings(ctx context.Context, page, limit int) ([]Filing, error) {
	return fetch.List[Filing](ctx, c.http, "/stable/institutional-ownership/latest", pageParams(page, limit))
}
func (c *Client) Extract(ctx context.Context, cik, year, quarter string) ([]Holding, error) {
	if strings.TrimSpace(cik) == "" || strings.TrimSpace(year) == "" || strings.TrimSpace(quarter) == "" {
		return nil, fmt.Errorf("fmp: cik, year, quarter must not be empty")
	}
	return fetch.List[Holding](ctx, c.http, "/stable/institutional-ownership/extract", map[string]string{"cik": cik, "year": year, "quarter": quarter})
}
func (c *Client) FilingDates(ctx context.Context, cik string) ([]FilingDate, error) {
	if strings.TrimSpace(cik) == "" {
		return nil, fmt.Errorf("fmp: cik must not be empty")
	}
	return fetch.List[FilingDate](ctx, c.http, "/stable/institutional-ownership/dates", map[string]string{"cik": cik})
}
func (c *Client) IndustrySummary(ctx context.Context, year, quarter string) ([]IndustrySummary, error) {
	if strings.TrimSpace(year) == "" || strings.TrimSpace(quarter) == "" {
		return nil, fmt.Errorf("fmp: year, quarter must not be empty")
	}
	return fetch.List[IndustrySummary](ctx, c.http, "/stable/institutional-ownership/industry-summary", map[string]string{"year": year, "quarter": quarter})
}
```
import: context, fmt, strings, internal/fetch.
- [ ] **Step 3:** fixtures = 각 카탈로그 JSON 예시 그대로(배열).
- [ ] **Step 4:** `filings_test.go`(헬퍼 calendar 패턴 정의): LatestFilings 파싱(CIK/Name) + delegation(path+page/limit) / Extract 파싱(Shares int, Value int) + delegation(path+cik/year/quarter) + 빈 cik 가드 / FilingDates 파싱(Year int64, Quarter int64) + path / IndustrySummary 파싱(IndustryValue int64) + delegation(path+year/quarter) + 빈 year 가드.
- [ ] **Step 5:** `unset GOROOT && go test ./form13f/ && go vet ./form13f/ && gofmt -l form13f/`. 커밋 `feat(form13f): Filings/Extract/Dates/IndustrySummary 4종`.

### Task 2: HolderAnalytics + IndustryBreakdown

**Files:** Create `form13f/analytics.go`, `form13f/analytics_test.go`, testdata `analytics-holder.json`, `industry-breakdown.json`.

- [ ] **Step 1:** 카탈로그(filings-extract-with-analytics-by-holder.md, holders-industry-breakdown.md) JSON READ → `HolderAnalytics`(~39필드) + `IndustryBreakdown`(12필드) struct 전수 매핑. 타입: marketValue/sharesNumber/changeInMarketValue/changeIn*Number/holdingPeriod/performance·lastPerformance·changeInPerformance → int64, weight*/changeInWeight*/ownership*/quarterEndPrice/avgPricePaid/*Percentage → float64, isNew/isSoldOut/isCountedForPerformance → bool, 텍스트 string. + 2 메서드:
```go
func (c *Client) ExtractAnalyticsByHolder(ctx context.Context, symbol, year, quarter string, page, limit int) ([]HolderAnalytics, error) {
	if strings.TrimSpace(symbol) == "" || strings.TrimSpace(year) == "" || strings.TrimSpace(quarter) == "" {
		return nil, fmt.Errorf("fmp: symbol, year, quarter must not be empty")
	}
	q := pageParams(page, limit)
	q["symbol"] = symbol
	q["year"] = year
	q["quarter"] = quarter
	return fetch.List[HolderAnalytics](ctx, c.http, "/stable/institutional-ownership/extract-analytics/holder", q)
}
func (c *Client) HoldersIndustryBreakdown(ctx context.Context, cik, year, quarter string) ([]IndustryBreakdown, error) {
	if strings.TrimSpace(cik) == "" || strings.TrimSpace(year) == "" || strings.TrimSpace(quarter) == "" {
		return nil, fmt.Errorf("fmp: cik, year, quarter must not be empty")
	}
	return fetch.List[IndustryBreakdown](ctx, c.http, "/stable/institutional-ownership/holder-industry-breakdown", map[string]string{"cik": cik, "year": year, "quarter": quarter})
}
```
- [ ] **Step 2:** fixtures = 카탈로그 예시 그대로.
- [ ] **Step 3:** `analytics_test.go`(헬퍼 재사용): HolderAnalytics 파싱(InvestorName!="", Weight float, MarketValue int64, IsNew/IsSoldOut bool 파싱) + delegation(path+symbol/year/quarter/page) + 빈 symbol 가드 / IndustryBreakdown 파싱(IndustryTitle!="", Weight float) + delegation(path+cik/year/quarter).
- [ ] **Step 4:** `go test ./form13f/ && go vet && gofmt -l`. 커밋 `feat(form13f): HolderAnalytics + IndustryBreakdown`.

### Task 3: HolderPerformance + PositionSummary

**Files:** Create `form13f/performance.go`, `form13f/performance_test.go`, testdata `holder-performance.json`, `positions-summary.json`.

- [ ] **Step 1:** 카탈로그(holder-performance-summary.md, positions-summary.md) JSON READ → `HolderPerformance`(~34필드) + `PositionSummary`(~39필드) struct 전수 매핑. 타입: portfolioSize/securities*/marketValue*/averageHoldingPeriod*/performance(절대,1/3/5year,sinceInception)/investorsHolding*/numberOf13Fshares*/totalInvested*/positions(new/increased/closed/reduced)*/totalCalls*/totalPuts* → int64, turnover*/*Percentage/ownershipPercent*/putCallRatio* → float64, 텍스트 string. + 2 메서드:
```go
func (c *Client) HolderPerformanceSummary(ctx context.Context, cik string, page int) ([]HolderPerformance, error) {
	if strings.TrimSpace(cik) == "" {
		return nil, fmt.Errorf("fmp: cik must not be empty")
	}
	return fetch.List[HolderPerformance](ctx, c.http, "/stable/institutional-ownership/holder-performance-summary", map[string]string{"cik": cik, "page": strconv.Itoa(page)})
}
func (c *Client) PositionsSummary(ctx context.Context, symbol, year, quarter string) ([]PositionSummary, error) {
	if strings.TrimSpace(symbol) == "" || strings.TrimSpace(year) == "" || strings.TrimSpace(quarter) == "" {
		return nil, fmt.Errorf("fmp: symbol, year, quarter must not be empty")
	}
	return fetch.List[PositionSummary](ctx, c.http, "/stable/institutional-ownership/symbol-positions-summary", map[string]string{"symbol": symbol, "year": year, "quarter": quarter})
}
```
import: context, fmt, strconv, strings, internal/fetch.
- [ ] **Step 2:** fixtures = 카탈로그 예시 그대로.
- [ ] **Step 3:** `performance_test.go`(헬퍼 재사용): HolderPerformance 파싱(InvestorName!="", PortfolioSize int64, Turnover float, PerformancePercentage1year float) + delegation(path+cik/page) + 빈 cik 가드 / PositionSummary 파싱(Symbol!="", InvestorsHolding int64, PutCallRatio float) + delegation(path+symbol/year/quarter) + 빈 symbol 가드.
- [ ] **Step 4:** `go test ./form13f/ && go vet && gofmt -l`. 커밋 `feat(form13f): HolderPerformance + PositionSummary`.

### Task 4: 루트 와이어 + README + examples + 통합테스트

**Files:** Modify `client.go`, `client_test.go`, `README.md`, `integration_test.go`; Create `examples/form13f/main.go`.

- [ ] **Step 1:** `client.go` — import `form13f`, struct 에 `Form13F *form13f.Client`, NewClient 에 `c.Form13F = form13f.New(hc)`. (gofmt -w client.go.)
- [ ] **Step 2:** `client_test.go` — `TestNewClient_HasForm13F`.
- [ ] **Step 3:** README 표 행 신규: `| Form 13F | \`client.Form13F\` | LatestFilings, Extract, FilingDates, ExtractAnalyticsByHolder, HolderPerformanceSummary, HoldersIndustryBreakdown, PositionsSummary, IndustrySummary — 8 endpoint |`.
- [ ] **Step 4:** `examples/form13f/main.go` — NewClientFromEnv → LatestFilings(0,5) 건수 + PositionsSummary("AAPL","2023","3") 첫 건 보유 기관 수 출력.
- [ ] **Step 5:** `integration_test.go` — `TestIntegration_Form13F`: LatestFilings(0,5) err 체크 / FilingDates("0001067983") err 체크 / PositionsSummary("AAPL","2023","3") err 체크 / IndustrySummary("2023","3") err 체크.
- [ ] **Step 6:** `unset GOROOT && go build ./... && go vet ./... && go test ./... && go build -tags integration ./... && gofmt -l .` 통과. 커밋 `feat(form13f): 루트 와이어 + README + examples + 통합테스트`.

## Self-Review 메모
- 8 endpoint: 작은 4=T1, HolderAnalytics+IndustryBreakdown=T2, HolderPerformance+PositionSummary=T3, 와이어/문서=T4.
- 대형 struct 카탈로그 전수 매핑. bool 3종(HolderAnalytics), year/quarter int64(FilingDate).
- 다중 필수 가드는 한 번에 검사.
